---
description: plan — the truth-and-presentation pass responding to the external (smart-tier) review of the published repo, verified first-hand 2026-07-21
type: plan
date: 2026-07-21
governs: []
status: complete
---

# plan-truth-and-presentation-2026-07-21

> **CLOSED COMPLETE 2026-07-21, after two rounds.** Round 1 (scope below): committed `4ad0183`/`a2eb174`, pushed. Round 2 (the external re-review of round 1, five findings verdicted first-hand): banned-identifier literals removed from the publication memo and STATUS (three occurrences this pass had itself introduced; the sweep-runs-last lesson); `settings.local.json` untracked for real (`21902df`; the first attempt was reversed by pathspec-commit semantics); working context redesigned to derive publication state from git; this closure note added (the round-1 omission was itself a finding); the double-heading claim refuted with a curl receipt. The incident earned a mechanism: the confidentiality sweep + pre-push hook, registered with falsifier and witness plan.

**APPROVED 2026-07-21 (the User: "approve, bank the plan and run it").** Genre 3A build/change, sized **S–M** (8 sub-tasks, additive plus one mechanism change). Posture: predictive (steps knowable; the external review supplied the findings and the primary verified each first-hand before this plan).

## Pattern Search Results

The external review's six findings, verified: gate marker non-consumption CONFIRMED (script read); published-stale CURRENT_FOCUS CONFIRMED (by construction of posture (i)); stale memo statuses CONFIRMED (two frontmatter fields + OPEN markers, two already carrying inline resolutions); public-surface gaps CONFIRMED where checkable (title-only, no meta description); stale README assembly note CONFIRMED; tracked `settings.local.json` CONFIRMED. Product points adopted: spirit one-pager (adoption gap), evidentiary-boundary sentence. Overstatement noted: cross-estate marker sharing unverified (names differ; template already parameterised).

## Scope (one slice, ordered)

1. **Gate script v2** (mechanism change): repo-scoped marker (hash of project dir) + consume-on-use. Falsifier pre-registered on the register row; both arms re-armed UNVERIFIED until witnessed. Estate CD #9f and canon template CD #9f wording updated (CD change under the User's slice approval).
2. **Status truing:** lineage-verdicts memo → approved; founding plan → complete with closure line; status semantics ("status is maintained at life events; OPEN markers are as-at-banking, resolutions arrive as dated notes") stated in the estate evidence-archive README, one clarifying sentence in the canon evidence-archive README.
3. **README:** stale assembly note removed; one evidentiary-boundary sentence added (receipted for the author, honestly not independently reproducible).
4. **Untrack `.claude/settings.local.json`** + gitignore entry (stays in history; contents non-sensitive).
5. **`docs/_config.yml`:** title, description, language for search/link previews.
6. **`adoption/THE_SPIRIT.md`:** the copyable Rung 1 one-pager; linked from GETTING_STARTED and the ladder.
7. Estate working context updated; **repo description/homepage/topics remain the User's GitHub-UI task.**
8. Two-lane sweep + adversarial review + dispositions; seam-scoped commits; **the push is the User's word at the end** (CD #4a; it also publishes the corrected working context, closing review finding 2).

## Risks

Mechanism change to a live gate (mitigated: falsifier + witness re-registration; behaviour-preserving block convention) · canon edits (sweep + review as ever) · untracking a tracked file (history preserved; local file untouched).

## Verification

Per-item receipts in the commit ledger; reviewer pass over the diff set; the register row carries the v2 witness plan.
